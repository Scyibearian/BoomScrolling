event_inherited();
if (hp <=0)
{
	//MAKE DRAWN SPRITE ROTATE
	//#todo
	//sprite_index = sprites.downed;
}
//else
//{
	//if (sprite_index == sprites.downed) sprite_index = sprites.idle; //Returns to idle if revived //Had to remove this so pcs animate
//}